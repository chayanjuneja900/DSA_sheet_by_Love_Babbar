#include <Bits/stdc++.h>
using namespace std;

void sort012(vector<int>& arr) {
        int ct_0=0,ct_1=0,ct_2=0;
        
        for(int i=0;i<arr.size();i++){
            if(arr[i]==0)ct_0++;
            if(arr[i]==1)ct_1++;
            if(arr[i]==2)ct_2++;
        }
        
        vector<int> ans;
        for(int i=0;i<ct_0;i++) {ans.push_back(0);}
        for(int i=0;i<ct_1;i++) {ans.push_back(1);}
        for(int i=0;i<ct_2;i++) {ans.push_back(2);}
        
        arr=ans;
        
    }

    int main(){
        
    }